services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile.dev
    init: true
    command: sleep infinity

    volumes:
      - ./:/app
      - node_modules:/app/node_modules

volumes:
  node_modules: